---
name: committee:custom
description: Assemble an ad-hoc committee from specific expert personas. Pick exactly who you want by their IDs, optionally add dynamic slots.
---

This is a sub-command of the Committee Review System. Read and follow `skills/committee/SKILL.md` with intent: **custom**.

The user has invoked `/committee:custom`. Parse the listed member IDs from arguments. Set dynamic slot count from `--add-members` flag (default 0). Proceed to Phase 0 (Intake & Mode Selection) with the custom roster.
